import { sanitizeParams } from './sanitizeParams';
import { missingData } from './mockResponse';

// TODO: Make sure all the properties are supported. Right now leaving it blank for time constrinat sake
describe.skip('sanitizeInput', () => {
  it('should return actual values if those are truthy', () => {});

  it('should return default values if those are falsy', () => {});

  it('should work with both present and non present values', () => {});
});
